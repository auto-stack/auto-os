// pages/slider.at - Slider component documentation page

widget SliderPage {
    model { activeTab str = "preview" }

    view {
        col {
            h1 "Slider"
            text "Slider lets users select a numeric value by dragging a handle along a track between a minimum and maximum. Configure the step size for discrete increments. Ideal for volume controls, price range filters, opacity sliders, and any setting with a bounded numeric range."

            h2 "Installation"
            codeblock (lang: "bash", code: "npx shadcn-vue@latest add slider") {}

            h2 "Simple"
            preview-card (id: "slider-basic") {
                slider (min: "0", max: "100", value: "50", style: "w-60") {}
            }

            h2 "Properties"
            table {
                thead {
                    tr {
                        th "Property"
                        th "Type"
                        th "Default"
                        th "Values"
                        th "Description"
                    }
                }
                tbody {
                    tr {
                        td "min"
                        td "number"
                        td "0"
                        td "-"
                        td "Minimum value"
                    }
                    tr {
                        td "max"
                        td "number"
                        td "100"
                        td "-"
                        td "Maximum value"
                    }
                    tr {
                        td "step"
                        td "number"
                        td "1"
                        td "-"
                        td "Step increment"
                    }
                }
            }
        }
    }
}
