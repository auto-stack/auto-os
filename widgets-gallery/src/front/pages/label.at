// pages/label.at - Label component documentation page

widget LabelPage {
    model { activeTab str = "preview" }

    view {
        col {
            h1 "Label"
            text "Label renders an accessible text label associated with a form control, enabling screen readers to announce the field's purpose when the user focuses it. Use the for attribute to link it to an input by id. Essential for form accessibility and usability."

            h2 "Installation"
            codeblock (lang: "bash", code: "npx shadcn-vue@latest add label") {}

            h2 "Simple"
            preview-card (id: "label-basic") {
                label "Email"
            }

            h2 "With Input"
            preview-card (id: "label-input") {
                col (style: "gap-1.5 w-64") {
                    label (text: "Email", for: "email") {}
                    input (id: "email", placeholder: "Enter your email") {}
                }
            }

            h2 "Properties"
            table {
                thead {
                    tr {
                        th "Property"
                        th "Type"
                        th "Default"
                        th "Values"
                        th "Description"
                    }
                }
                tbody {
                    tr {
                        td "text"
                        td "string"
                        td "-"
                        td "-"
                        td "Label text content (slot)"
                    }
                    tr {
                        td "for"
                        td "string"
                        td "-"
                        td "-"
                        td "ID of the associated form control"
                    }
                }
            }
        }
    }
}
