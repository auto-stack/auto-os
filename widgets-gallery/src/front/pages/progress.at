// pages/progress.at - Progress component documentation page

widget ProgressPage {
    model { activeTab str = "preview" }

    view {
        col {
            h1 "Progress"
            text "Progress displays a horizontal bar indicating the completion status of a task — file uploads, multi-step forms, or background processing. The bar fills proportionally from 0 to 100% based on the value prop. Use it to give users confidence that an operation is proceeding."

            h2 "Installation"
            codeblock (lang: "bash", code: "npx shadcn-vue@latest add progress") {}

            h2 "Simple"
            preview-card (id: "progress-basic") {
                progress (value: "33", style: "w-60") {}
            }

            h2 "Properties"
            table {
                thead {
                    tr {
                        th "Property"
                        th "Type"
                        th "Default"
                        th "Values"
                        th "Description"
                    }
                }
                tbody {
                    tr {
                        td "value"
                        td "number"
                        td "0"
                        td "0-100"
                        td "Progress percentage"
                    }
                    tr {
                        td "max"
                        td "number"
                        td "100"
                        td "-"
                        td "Maximum value"
                    }
                }
            }
        }
    }
}
