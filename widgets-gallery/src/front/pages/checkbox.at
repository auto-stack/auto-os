// pages/checkbox.at - Checkbox component documentation page

widget CheckboxPage {
    model { activeTab str = "preview" }

    view {
        col {
            h1 "Checkbox"
            text "Checkbox lets users select one or more options from a set, or confirm a binary choice like accepting terms. Unlike a Switch (which represents an on/off state), a checkbox implies a yes/no decision within a form or list. Supports checked, unchecked, and disabled states."

            h2 "Installation"
            codeblock (lang: "bash", code: "npx shadcn-vue@latest add checkbox") {}

            h2 "Simple"
            preview-card (id: "checkbox-basic") {
                row (style: "gap-2 items-center") {
                    checkbox (id: "terms1") {}
                    label (text: "Accept terms and conditions", for: "terms1") {}
                }
            }

            h2 "Disabled"
            preview-card (id: "checkbox-disabled") {
                row (style: "gap-2 items-center") {
                    checkbox (id: "terms2", disabled: "true") {}
                    label (text: "Accept terms and conditions", for: "terms2") {}
                }
            }

            h2 "Checked"
            preview-card (id: "checkbox-checked") {
                row (style: "gap-2 items-center") {
                    checkbox (id: "terms3", checked: "true") {}
                    label (text: "Accept terms and conditions", for: "terms3") {}
                }
            }

            h2 "Properties"
            table {
                thead {
                    tr {
                        th "Property"
                        th "Type"
                        th "Default"
                        th "Values"
                        th "Description"
                    }
                }
                tbody {
                    tr {
                        td "checked"
                        td "boolean"
                        td "false"
                        td "true, false"
                        td "Checkbox state"
                    }
                    tr {
                        td "disabled"
                        td "boolean"
                        td "false"
                        td "true, false"
                        td "Disables the checkbox"
                    }
                    tr {
                        td "id"
                        td "string"
                        td "-"
                        td "-"
                        td "ID for label association"
                    }
                }
            }
        }
    }
}
