widget CardSuit (suit: str = "", style: str = "") {
    view {
        col {
            class: .style
            if .suit == "spade" {
                icon (name: "spade", class: .style) {}
            } else {
                if .suit == "heart" {
                    icon (name: "heart", class: .style) {}
                } else {
                    if .suit == "diamond" {
                        icon (name: "diamond", class: .style) {}
                    } else {
                        if .suit == "club" {
                            icon (name: "club", class: .style) {}
                        }
                    }
                }
            }
        }
    }
}
